# Copy this file to .env.local and fill in real values.
# .env.local is git-ignored (see .gitignore: .env.*.local) — never commit it.
#
# Get these from: supabase secrets list (run yourself; don't paste output into chat)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENROUTER_API_KEY=
FDC_API_KEY=
